// Docs now use the sidebar navigation from the root layout
// Individual doc pages are wrapped automatically with proper breadcrumbs

export default function DocsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
